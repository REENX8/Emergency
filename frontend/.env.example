# Frontend environment variables — copy to .env and adjust for your environment.
# In dev, the CRA "proxy" field in package.json forwards /api requests to localhost:8000,
# so REACT_APP_API_URL can be left empty for local dev if desired.

REACT_APP_API_URL=http://localhost:8000
